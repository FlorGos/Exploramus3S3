import 'package:flutter/material.dart';

class PedometerPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Podomètre'),
      ),
      body: Center(
        child: Text('Page du podomètre - À implémenter'),
      ),
    );
  }
}

