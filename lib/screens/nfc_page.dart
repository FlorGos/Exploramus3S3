import 'package:flutter/material.dart';

class NFCPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('NFC'),
      ),
      body: Center(
        child: Text('Page NFC - À implémenter'),
      ),
    );
  }
}

